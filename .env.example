# --- Основные настройки Django ---
DEBUG=False
SECRET_KEY=qvo*9ug4z3r9vxuun07y202ql_x=+qh1qdnsjrfn6j&g1=59v0
ALLOWED_HOSTS=.twc.run
DJANGO_SETTINGS_MODULE=pet_project.settings
PYTHONPATH=/app/backend

# --- CORS / CSRF / Frontend ---
CORS_ALLOWED_ORIGINS=https://petmarket.twc.run
CSRF_TRUSTED_ORIGINS=https://petmarket.twc.run
FRONTEND_URL=https://petmarket.twc.run

# --- База данных (Timeweb автоматически подставляет, но можно явно указать при необходимости) ---
# DB_NAME=      ← не нужно указывать вручную, Timeweb сам прокинет
# DB_USER=
# DB_PASSWORD=
# DB_HOST=
# DB_PORT=

# --- Redis (если используется) ---
# REDIS_HOST=   ← если используется, укажите хост из сервиса Redis в Timeweb
# REDIS_PORT=   ← обычно 6379

# --- Почта (Яндекс) ---
EMAIL_HOST=smtp.yandex.ru
EMAIL_PORT=465
EMAIL_USE_SSL=True
EMAIL_HOST_USER=reazordd@yandex.ru
EMAIL_HOST_PASSWORD=bpelzaibnborpvxa
DEFAULT_FROM_EMAIL=reazordd@yandex.ru

# --- Superuser (только при первом запуске через миграции, необязательно в рантайме) ---
# DJANGO_SUPERUSER_USERNAME=admin
# DJANGO_SUPERUSER_PASSWORD=5v1234567
# DJANGO_SUPERUSER_EMAIL=admin@example.com